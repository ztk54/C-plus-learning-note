## 类的定义
```cpp
class Date
{
public:
	void Init(int year,int month,int day)
	{
		_year=year;
		_month=month;
		_day=day;
	}
	
private:
	int _year;
	int _month;
	int _day;
};(注意类的最后要加上一个分号)
```
### 访问限定词
| 限定符         | 类外能否访问 | 说明                         |
| ----------- | ------ | -------------------------- |
| `public`    | ✅      | 公开的接口                      |
| `protected` | ❌      | 现在和 private 一样，**继承**章节才区分 |
| `private`   | ❌      | 私有                         |

1. 访问权限的作用域：**从该限定符开始，到下一个限定符或 `}` 结束**。
2. `class` 默认 `private`，`struct` 默认 `public`（这是 C++ 里两者**唯一的本质区别**，其他都一样）。
3. 一般把**成员变量设为 private**，需要给别人用的函数设为 public → 这就是**封装**。

```cpp
class A { int a; };    // a 是 private
struct B { int b; };   // b 是 public
```

 **踩坑**：类外访问私有成员 → **C2248：无法访问 private 成员**。
📌 **补全：封装的意义**（截图只给了结论，没展开）
- **把数据和方法打包**，外部只能通过你开放的接口操作数据；
- **可以随时改内部实现**，只要接口不变，用的人不受影响；
- **能加检查**：比如 `SetMonth(int m)` 里判断 `1 <= m <= 12`，直接暴露成员变量就做不到。
## 类的实例化
**类 = 设计图，对象 = 按图造出来的房子。**
```cpp
class Date
{
public:
    int _year;    // 这里只是"声明有哪些成员"，还没分配空间
};

int main()
{
    Date d1;          // ← 这一步才分配空间，叫"实例化"
    Date d2(2024, 4, 26);
    Date* pd = &d1;
    Date& rd = d1;

    cout << sizeof(Date) << endl;   // 12（三个 int）
    cout << sizeof(d1) << endl;     // 12
    return 0;
}
```
要点：
- 类本身**不占存储空间**，只有实例化出的对象才占。
- 一个类可以实例化出多个对象，各自有独立的成员变量（成员函数**共享**一份，放在代码段）。

 **进阶：空类的大小是 1**
```cpp
class Empty {};              // 里面什么都没有
cout << sizeof(Empty) << endl;   // 1，不是 0
```
原因：C++ 要求每个对象必须有**唯一地址**，占 1 字节占位。
## 内存对齐
### 规则
1. 第一个成员在偏移量 **0** 处。
2. 其他成员要对齐到**对齐数**的整数倍处。
   **对齐数 = min(编译器默认对齐数, 该成员大小)**（**VS 默认是 8**，Linux gcc 没有默认对齐数限制）。
3. **结构体总大小 = 最大对齐数的整数倍**。
4. 如果是**嵌套结构体**，嵌套的那个结构体对齐到自己内部的最大对齐数。
###  逐题计算

```cpp
struct A { char _c;  int _i;  };                
// 1 + 3pad + 4      = 8

struct B { int _i;   char _c; };                
// 4 + 1 + 3pad      = 8

struct C { char _c1; char _c2; int _i; };       
// 1+1 + 2pad + 4    = 8

struct D { double _d; char _c; int _i; };       
// 8 + 1+3pad + 4    = 16

struct E { char _c;  struct A _a; };            
// 1 + 3pad + 8      = 12

struct F { char _c;  double _d; char _c2; };    
// 1+7pad + 8 + 1+7pad = 24
```

以 `struct F` 为例：

```cpp
偏移 0  : char  _c     (1 字节)

偏移 1-7: 填充 (7 字节)   ← double 要求 8 的倍数，下一个空位是 8

偏移 8  : double _d    (8 字节)

偏移 16 : char  _c2    (1 字节)

偏移 17-23: 填充 (7 字节)  ← 总大小要是最大对齐数 8 的倍数
=> sizeof(F) == 24

```

> **为什么要对齐**：CPU 从内存读数据是按块读的，不对齐就要读两次再拼，性能损失很大。所以是**拿空间换时间**。
### 11.3 修改默认对齐数

```cpp
#pragma pack(1)          // 改成 1 字节对齐
struct S { char _c; int _i; };   // sizeof(S) == 5
#pragma pack()           // 恢复默认
```
📌 **补全：查看偏移量**
```cpp
#include <cstddef>
printf("%d\n", (int)offsetof(struct F, _d));   // 8
```
## this指针
### this指针是什么
在使用类的时候，我们的编译器会默认把对应的对象隐式传参，比如说d1.Init(2025,1,1)实际上我们会把d1的地址传递过去，也就是所谓的this指针。
```cpp
class Date
{
public:
	void Init(int year,int month,int day)
	{
		_year=year;
		_month=month;
		_day=day;
	}
private:
	int _year;
	int _month;
	int _day; 
};

//编译器看到的
void Init(Date* const this, int year, int month, int day)
{
    this->_year = year;
    this->_month = month;
    this->_day = day;
}

```
### 三条使用规则
1. **不能**在形式参数位置显示写this：`void Init(Date* const this, ...)`
2. **不能**在实参位置显式传参this：`d1.Init(&d1,2024,1,1)`
3. **可以**在函数体内显示使用
### 经典题目
**题目**：下面代码的运行结果？
```cpp
class A
{
public:
    void Print()  { cout << "Print()" << endl; }       
    void PrintA() { cout << _a << endl; }               
private:
    int _a = 1;
};

int main()
{
    A* p = nullptr;
    p->Print();      // ①
    p->PrintA();     // ②
    return 0;
}
```
> [!tips]
> 这里我们就要使用到this指针了，第一个相当于把一个空指针传给了this，函数体没有解引用空指针，因此不会报错。
> 第二个是需要访问p中的元素_a，相当于对空指针解引用了，所以内存崩溃。**

### 返回 `*this` 支持链式调用**
```cpp
class Date
{
public:
    Date& AddDay() 
    {     _day += 1; 
		  return *this; 
    }   
    // 返回当前对象本身

private:
    int _year = 1, _month = 1, _day = 1;
};

d1.AddDay().AddDay().AddDay();    // 连续调用

```
这也是后面 `operator=`、`operator+=` 都返回 `Date&` 的原因。
## 构造函数